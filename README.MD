# Start the DB
    python
    from db.database import create_db_tables
    create_db_tables()
    exit()

# activate the virtual environment
    source venv/bin/activate  # On Windows use `venv\Scripts\activate`    

# Start the FastAPI as Dev
    fastapi dev fastapi/main.py
                                                                                                                                             
